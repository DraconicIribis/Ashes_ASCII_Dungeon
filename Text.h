#pragma once

#include <iostream>

using namespace std;

void textType(string output, bool reverse);
void textReplace(string prevLine, string output, bool typeOut);
void clearScreen();
int setInput(int min, int max);